演示文件是".jar"格式的
是将Java代码打包生成的

每个阶段的演示都是代码完成后，程序执行的样子。

你有两种方式打开它：
1.直接双击打开，看到的就是GUI登陆界面。

2.如果双击打开不生效，你可以先配置JDK环境变量，然后在存放".jar"文件的位置打开命令行（cmd），然后输入命令：
"java -jar stageXxx.jar"

其中stageXxx.jar表示演示文件的名字。
注：在命令行中输入文件名，可以先输入前面部分字符串，然后按Tab键即可自动补全。

如果实在无法打开，建议直接询问老师。
